<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>螢夏祭 2027 | 滋賀県立守山高等学校</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        .hover-lift:hover { transform: translateY(-5px); transition: 0.3s; }
    </style>
</head>
<body class="bg-gray-50 text-gray-800">

    <!-- ヘッダー -->
    <header class="bg-white sticky top-0 z-50 shadow-sm">
        <nav class="max-w-6xl mx-auto px-6 py-4 flex items-center justify-between">
            <div class="flex items-center gap-3">
                <img src="https://www.shiga-ec.ed.jp/moriyama-h/images/logo.png" alt="守山高校ロゴ" class="h-10 w-auto">
                <span class="text-xl font-bold text-emerald-800">螢夏祭 2027</span>
            </div>
            <div class="hidden md:flex gap-8 font-medium">
                <a href="#news" class="hover:text-emerald-600">お知らせ</a>
                <a href="#timetable" class="hover:text-emerald-600">タイムテーブル</a>
                <a href="#downloads" class="hover:text-emerald-600">ダウンロード</a>
            </div>
        </nav>
    </header>

    <!-- メインビジュアル & カウントダウン -->
    <section class="bg-emerald-900 text-white py-20 text-center">
        <h1 class="text-4xl md:text-6xl font-bold mb-6">螢夏祭 2027</h1>
        <div id="countdown" class="flex justify-center gap-4 text-2xl md:text-4xl font-mono">
            <div class="bg-emerald-800 p-4 rounded-lg"><span id="days">00</span><br><small class="text-sm">Days</small></div>
            <div class="bg-emerald-800 p-4 rounded-lg"><span id="hours">00</span><br><small class="text-sm">Hours</small></div>
            <div class="bg-emerald-800 p-4 rounded-lg"><span id="minutes">00</span><br><small class="text-sm">Mins</small></div>
        </div>
    </section>

    <!-- コンテンツエリア -->
    <main class="max-w-6xl mx-auto p-6 grid md:grid-cols-2 gap-10 mt-10">
        <section id="news" class="bg-white p-6 rounded-xl shadow-sm">
            <h2 class="text-2xl font-bold mb-4 text-emerald-800"><i class="fa fa-bell mr-2"></i>お知らせ</h2>
            <div id="news-list" class="space-y-4 text-gray-600">投稿データがここに同期されます。</div>
        </section>
        
        <section id="timetable" class="bg-white p-6 rounded-xl shadow-sm">
            <h2 class="text-2xl font-bold mb-4 text-emerald-800"><i class="fa fa-calendar mr-2"></i>タイムテーブル</h2>
            <div id="table-list" class="space-y-4">データ読み込み中...</div>
        </section>
    </main>

    <!-- フッター -->
    <footer class="bg-gray-900 text-gray-400 py-12 mt-16 text-center">
        <div class="max-w-4xl mx-auto px-6">
            <img src="https://www.shiga-ec.ed.jp/moriyama-h/images/logo.png" alt="Logo" class="h-16 mx-auto mb-6 invert opacity-50">
            <h3 class="text-white text-lg font-bold mb-2">滋賀県立守山高等学校 螢夏祭</h3>
            <p>〒524-0012 滋賀県守山市播磨田町1100</p>
            <p>Tel: 077-583-2001</p>
            <div class="mt-6 flex justify-center gap-6 text-2xl">
                <a href="#" class="hover:text-pink-500"><i class="fab fa-instagram"></i></a>
            </div>
            <p class="mt-8 text-sm">&copy; 2027 Moriyama High School All Rights Reserved.</p>
        </div>
    </footer>

    <script>
        <!-- 以下のスクリプトを</body>の直前に追記 -->
<script type="module">
  // 1. SDKの読み込み
  import { initializeApp } from "https://www.gstatic.com/firebasejs/10.7.1/firebase-app.js";
  import { getFirestore, collection, getDocs } from "https://www.gstatic.com/firebasejs/10.7.1/firebase-firestore.js";

  // 2. Firebaseの設定（Firebaseコンソールからコピーしたものを貼る）
// Import the functions you need from the SDKs you need
import { initializeApp } from "firebase/app";
import { getAnalytics } from "firebase/analytics";
// TODO: Add SDKs for Firebase products that you want to use
// https://firebase.google.com/docs/web/setup#available-libraries

// Your web app's Firebase configuration
// For Firebase JS SDK v7.20.0 and later, measurementId is optional
const firebaseConfig = {
  apiKey: "AIzaSyBgc1s4gVtSwGX1sYI-hYRwf1Gvv5qgFr8",
  authDomain: "keikasai-ed701.firebaseapp.com",
  projectId: "keikasai-ed701",
  storageBucket: "keikasai-ed701.firebasestorage.app",
  messagingSenderId: "747502295447",
  appId: "1:747502295447:web:e1dc3ad863c9ed7cc8ccd2",
  measurementId: "G-CTLQB6XJ3N"
};

// Initialize Firebase
const app = initializeApp(firebaseConfig);
const analytics = getAnalytics(app);

  // 3. データの取得と表示
  async function loadNews() {
    const querySnapshot = await getDocs(collection(db, "news"));
    const container = document.getElementById('news-list');
    container.innerHTML = ""; // 読み込み中の文字を消す

    querySnapshot.forEach((doc) => {
      const data = doc.data();
      container.innerHTML += `
        <div class="border-b pb-2">
            <h3 class="font-bold text-emerald-900">${data.title}</h3>
            <p>${data.content}</p>
        </div>
      `;
    });
  }

  loadNews();
</script>
        // カウントダウン処理
        function updateTimer() {
            const eventDate = new Date("2027-09-01T09:00:00").getTime();
            const now = new Date().getTime();
            const diff = eventDate - now;
            document.getElementById('days').innerText = Math.floor(diff / (1000 * 60 * 60 * 24));
            document.getElementById('hours').innerText = Math.floor((diff % (1000 * 60 * 60 * 24)) / (1000 * 60 * 60));
            document.getElementById('minutes').innerText = Math.floor((diff % (1000 * 60 * 60)) / (1000 * 60));
        }
        setInterval(updateTimer, 1000);
        updateTimer();
    </script>
</body>
</html>
